import { useEffect } from "react";

/**
 * MapAutoFit
 * Stub component to satisfy App.jsx import.
 * Does nothing by design.
 */
export default function MapAutoFit() {
  useEffect(() => {
    // intentionally empty
  }, []);

  return null;
}
